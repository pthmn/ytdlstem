fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
yt-dlp>=2024.1.0
demucs>=4.0.0
torch>=2.0.0
torchaudio>=2.0.0
pydub>=0.25.1
aiofiles>=23.0.0
# spotdl is installed separately (conflicts with FastAPI version)
# It's used as a CLI tool via subprocess only
